# Server runtime
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Add your MongoDB Atlas connection string when database work begins.
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/repopulse

# Add this only when Gemini-powered features are implemented.
GEMINI_API_KEY=your_gemini_api_key
